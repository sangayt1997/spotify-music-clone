import React from "react";
import Box from "@mui/material/Box";

const Albums = () => {

    return (
        <Box sx={{display: 'flex'}}>
            <p>Albums Page</p>
        </Box>
    );
}

export default Albums;
