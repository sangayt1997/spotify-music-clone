import styled from "styled-components";
import Box from "@mui/material/Box";

export const PeriodCategoryContainer = styled(Box)`
  .period-title {
    font-size: 32px;
    font-weight: 700;
    padding: 12px 0;
  }
`;
